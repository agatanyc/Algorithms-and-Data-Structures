#def print(root):
#    current_level = None
#    next_level = None
#    if not root:
#        return current_level
#    else:
#        root = current_level

class Tree:
    def __init__(self, value, left, right):
        self.value = value
        self.left = left
        slef.right = right

tree_1 = Tree(1, Tree(2, Tree(4,None, None), Tree(5, None, None)), Tree(3,
    Tree(6, None, None), Tree(7, None, None)))

def print(tree):
    while value:
        print(self.value)
        r = []
        r.append(self.left, self.right)
        if self.left:


